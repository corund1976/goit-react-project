const getUserEmail = state => state.user.userEmail;
const getUserId = state => state.user.userId;
// eslint-disable-next-line import/no-anonymous-default-export
export default {
  getUserEmail,
  getUserId,
}
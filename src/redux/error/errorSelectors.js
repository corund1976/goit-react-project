const getErrorMessage = state => state.error.errorMessage;
const getErrorStatus = state => state.error.errorStatus;
// eslint-disable-next-line import/no-anonymous-default-export
export default {
  getErrorMessage,
  getErrorStatus,
}
